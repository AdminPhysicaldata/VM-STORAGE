# Lot journalier poste 6 : construit ~5 Go (sessions de la veille) → pending_validation.
# La copie SFTP est déposée et le backend notifié ; la validation se fait sur /batch.
0 3 * * * root /usr/local/bin/python /app/build_daily_batch.py >> /proc/1/fd/1 2>&1

# Envoi des lots VALIDÉS à Mistral (réutilise SessionsToMistral via --only).
# Toutes les 10 min : dès qu'un lot est validé sur le site, il part au prochain passage.
*/10 * * * * root /usr/local/bin/python /app/send_validated_batches.py >> /proc/1/fd/1 2>&1
